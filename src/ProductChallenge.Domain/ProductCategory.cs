namespace ProductChallenge.Domain;

/// <summary>
/// Valores explícitos a partir de 1: reordenar o enum não reinterpreta registros já gravados.
/// </summary>
public enum ProductCategory
{
    Electronics = 1,
    Groceries = 2,
    Apparel = 3,
    HomeAndGarden = 4,
    Toys = 5
}
